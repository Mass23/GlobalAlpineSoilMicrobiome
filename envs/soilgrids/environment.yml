channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.12
  - rasterio>=1.3      # pulls in GDAL (with curl, used to stream CHELSA files)
  - numpy
  - pandas>=2.0
  - requests
  - soilgrids          # SoilGrids WCS services (pulls in owslib, rioxarray)
